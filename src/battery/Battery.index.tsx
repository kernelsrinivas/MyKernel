import React from 'react';
import { View, Text } from 'react-native';

export default function Battery(){
    return (<View style={{flex:1, justifyContent:'center', alignItems:'center'}}><Text>Battery</Text></View>)
}